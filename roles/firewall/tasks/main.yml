---
- task: Backup Current IPTables Configuration
  command: iptables-save > /etc/iptables/rules.v4

- task: Secure that shit (lite)
  include: iptables_lite.yml
  when: scoring_ip is not defined

- task: Secure that shit (heavy)
  include: iptables_heavy.yml
  when: scoring_ip is defined # or it's a workstation